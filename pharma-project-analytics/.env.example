# Pharma Project Analytics does not require any API keys, database
# credentials, or authentication secrets - it has no LLM, no database,
# and no login. This file is a placeholder only.
#
# Optional: override the maximum accepted upload size (in megabytes).
# MAX_UPLOAD_SIZE_MB=10
